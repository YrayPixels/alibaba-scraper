# Server Configuration
PORT=8000
NODE_ENV=production

# Puppeteer/Browser Configuration
# Set to true for production (headless mode)
PUPPETEER_HEADLESS=true

# Chrome/Chromium executable path
# Production (Railway/DigitalOcean): /usr/bin/google-chrome-stable
# macOS: /Applications/Google Chrome.app/Contents/MacOS/Google Chrome
# Linux: /usr/bin/google-chrome-stable or /usr/bin/chromium-browser
PUPPETEER_EXECUTABLE_PATH=/usr/bin/google-chrome-stable

# Scraping Method
# Set to false to use fetch-based scraping instead of browser (Puppeteer)
USE_BROWSER_SCRAPING=true

# Redis Cache Configuration (Optional but Recommended)
# The service works without Redis, but caching significantly improves performance

# Option 1: Use Redis URL (recommended for cloud providers like Railway, Heroku)
# Format: redis://[:password@]host[:port][/db-number]
REDIS_URL=redis://localhost:6379
# Example with password: redis://:your_password@redis.example.com:6379

# Option 2: Use individual connection parameters (if not using REDIS_URL)
# REDIS_HOST=localhost
# REDIS_PORT=6379
# REDIS_PASSWORD=your_password_here

# Cache TTL (Time To Live) in seconds
# Default: 21600 (6 hours)
# This prevents serving stale product data while maintaining good performance
# 
# Recommended values:
# - 14400 (4 hours) - More frequent updates, fresher data
# - 21600 (6 hours) - Balanced (default) - good for most use cases
# - 43200 (12 hours) - Longer cache, better performance, but data may be slightly stale
# - 86400 (24 hours) - Maximum performance, but data may be stale
#
# Note: Product prices, stock availability, and details can change frequently.
# Shorter TTL = fresher data but more scraping requests
# Longer TTL = better performance but potentially stale data
CACHE_TTL_SECONDS=21600
